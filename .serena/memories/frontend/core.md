# Frontend map
- `web/`: Vue 3 management UI using Vue Router, Vite, and Arco Design Vue.
- Follow existing Vue component and Arco patterns; keep API JSON names stable.
- Any backend API field/default change may require handler tests, docs/config examples, and UI updates.
- Commands and completion checks: `mem:suggested_commands`, `mem:task_completion`.