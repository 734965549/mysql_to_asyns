apiVersion: v1
kind: Service
metadata:
  name: mysql-to-sync
  labels:
    app: mysql-to-sync
spec:
  type: NodePort
  selector:
    app: mysql-to-sync
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
